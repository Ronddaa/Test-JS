const courseTopic = "JavaScript essentials";
// Change code below this line

const courseTopicLength = courseTopic;
const firstElement = courseTopic;
const lastElement = courseTopic;

console.log(courseTopicLength.length);
console.log(courseTopicLength[0]);
console.log(courseTopicLength.length - 1);

// Change code above this line

function getSubstring(string, length) {
    const substring = getSubstring.slice(getSubstring.length); // Change this line

    return substring;
}

function formatMessage(message, maxLength) {
  let result;
  // Change code below this line
  if (message <= maxLength) {
    message
  } else {
    message.slice(0, maxLength) + "..."
  }
  /// Change code above this line
  return result;
}
